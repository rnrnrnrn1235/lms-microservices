
server.port=8080
spring.application.name=gateway

# Gateway downstream service URIs (can be overridden per environment)
gateway.auth-service-uri=http://localhost:8081
gateway.library-service-uri=http://localhost:8082